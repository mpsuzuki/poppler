language: cpp

script:
  - dpkg -l
#  - ./autogen.sh
#  - ./configure
#  - fgrep GLIB_MKENUMS glib/Makefile
#  - make V=1
#  - mkdir "$PWD/root"
#  - make DESTDIR="$PWD/root" install
#  - make V=1 dist

matrix:
  include:
    - dist: trusty
      compiler: gcc     
addons:
  apt:
    sources:
      - sourceline: 'ppa:aacid/openjp2trusty'
      - sourceline: 'ppa:ricotz/testing'
    packages:
      - autoconf
      - automake
      - autotools-dev
      - libtool
      - gtk-doc-tools
      - gobject-introspection
      - libglib2.0-dev
      - libgtk2.0-dev
      - libfontconfig1-dev
      - libcairo2-dev
      - libjpeg-dev
      - libpng-dev
      - libtiff-dev
      - liblcms2-dev
      - libfreetype6-dev
      - libopenjp2-7-dev
      - libgirepository1.0-dev
